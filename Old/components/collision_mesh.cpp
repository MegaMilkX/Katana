#include "collision_shape.hpp"

void CollisionMesh::_editorGui() {
    /*
    if(ImGui::Button("Make from model")) {
        makeFromModel(collider->getObject()->get<Model>());
        collider->refreshShape();
    } */   
    return false;
}